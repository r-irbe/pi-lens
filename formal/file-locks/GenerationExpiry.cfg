\* expect: violated MutualExclusion
\* module: GenerationLock
\* A live holder past the stale threshold is superseded: the lease, as today.
CONSTANTS
    Procs = {p1, p2, p3}
    MaxCrashes = 0
    AllowExpiry = TRUE
    MaxGen = 5
    Cleanup = TRUE
    Recheck = TRUE
    Rounds = 2
    ListedMarker = FALSE
SPECIFICATION Spec
SYMMETRY Symmetry
INVARIANTS MutualExclusion NoLostRegistration NoOrphanLock
CHECK_DEADLOCK FALSE
