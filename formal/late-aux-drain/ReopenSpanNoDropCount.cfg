\* expect: violated NoFreshWithheld
\* module: LateAuxDrain
\* Mutant: the counts span the close but a publish dropped while closed is not counted, so the binding waits for a publish that already came.
CONSTANTS
    AgentTouches = 3
    ExternalEdits = 0
    MaxDrains = 3
    MaxRearms = 2
    Tol = 0
    AllowDefer = TRUE
    AllowCancel = FALSE
    PublishedAtCheck = TRUE
    MtimeGate = TRUE
    RefreshOnStale = FALSE
    CountBind = TRUE
    MarkAtNotify = TRUE
    ExtDuringGrace = FALSE
    Refresh = "none"
    RefreshRebaseline = TRUE
    RefreshOvertake = FALSE
    AllowSave = FALSE
    SaveExpect = 1
    Closes = 1
    Carry = "spanNoDrop"
SPECIFICATION Spec
INVARIANTS NoFreshWithheld
CHECK_DEADLOCK FALSE
