\* expect: violated NoFreshWithheld
\* module: LateAuxDrain
\* Admitted (#3482): a scanner that skips a superseded scan never publishes for it, so the backlog binding withholds the current answer until the rearm ceiling. opengrep@1a5fd9d runs every scan it is sent.
CONSTANTS
    AgentTouches = 2
    ExternalEdits = 0
    MaxDrains = 3
    MaxRearms = 2
    Tol = 0
    AllowDefer = TRUE
    AllowCancel = TRUE
    PublishedAtCheck = TRUE
    MtimeGate = TRUE
    RefreshOnStale = FALSE
    CountBind = TRUE
    MarkAtNotify = TRUE
    ExtDuringGrace = FALSE
    Refresh = "none"
    RefreshRebaseline = TRUE
    RefreshOvertake = FALSE
    AllowSave = FALSE
    SaveExpect = 1
    Closes = 0
    Carry = "span"
SPECIFICATION Spec
INVARIANTS NoFreshWithheld
CHECK_DEADLOCK FALSE
