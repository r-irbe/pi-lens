\* expect: pass
\* module: LateAuxDrain
\* Fixed (#3482): an external edit lands inside the aux-grace wait; markedAtMs is the touch entry, before its notify (index.ts collect-later mark, startedAt), so the edit's mtime postdates the baseline and the gate calls the scan stale.
CONSTANTS
    AgentTouches = 1
    ExternalEdits = 1
    MaxDrains = 3
    MaxRearms = 2
    Tol = 0
    AllowDefer = FALSE
    AllowCancel = FALSE
    PublishedAtCheck = TRUE
    MtimeGate = TRUE
    RefreshOnStale = FALSE
    CountBind = TRUE
    MarkAtNotify = TRUE
    ExtDuringGrace = TRUE
    Refresh = "none"
    RefreshRebaseline = FALSE
    RefreshOvertake = FALSE
    AllowSave = FALSE
    SaveExpect = 1
    Closes = 0
    Carry = "span"
SPECIFICATION Spec
INVARIANTS NoStaleFindings
CHECK_DEADLOCK FALSE
