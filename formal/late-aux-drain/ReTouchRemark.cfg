\* expect: pass
\* module: LateAuxDrain
\* Fixed (#3482): a second agent touch re-marks the pair while the first scan is outstanding; the backlog binding holds the version-less first publish until the re-mark's own scan has published.
CONSTANTS
    AgentTouches = 2
    ExternalEdits = 0
    MaxDrains = 3
    MaxRearms = 2
    Tol = 0
    AllowDefer = TRUE
    AllowCancel = TRUE
    PublishedAtCheck = TRUE
    MtimeGate = TRUE
    RefreshOnStale = FALSE
    CountBind = TRUE
    MarkAtNotify = TRUE
    ExtDuringGrace = FALSE
    Refresh = "none"
    RefreshRebaseline = FALSE
    RefreshOvertake = FALSE
    AllowSave = FALSE
    SaveExpect = 1
    Closes = 0
    Carry = "span"
SPECIFICATION Spec
INVARIANTS NoStaleFindings
CHECK_DEADLOCK FALSE
