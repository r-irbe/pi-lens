\* expect: violated NoSilentLoss
\* module: IdleReset
\* Fix mutant: only the no-client return is marked; touchFile's checkDestroyed() early return still drops.
CONSTANTS
    Sweep = FALSE
    SweepOverrun = FALSE
    Reaping = FALSE
    Cascade = TRUE
    WarmAttach = FALSE
    HoldCheck = TRUE
    FixParts = {"acquire"}
SPECIFICATION Spec
INVARIANTS TypeOK NoSilentLoss
CHECK_DEADLOCK FALSE
