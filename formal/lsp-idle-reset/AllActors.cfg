\* expect: pass
\* module: IdleReset
\* Fixed (#3483): sweep, cascade, warm attach and reaping (sweep in range) together; a cascade neighbour lost to a destroyed service is kept as inconclusive.
CONSTANTS
    Sweep = TRUE
    SweepOverrun = FALSE
    Reaping = TRUE
    Cascade = TRUE
    WarmAttach = TRUE
    HoldCheck = TRUE
    FixParts = {"entry","acquire"}
SPECIFICATION Spec
INVARIANTS TypeOK NoResetUnderHold NoResetDuringSweep HonestSweepLabel NoSilentLoss
CHECK_DEADLOCK FALSE
