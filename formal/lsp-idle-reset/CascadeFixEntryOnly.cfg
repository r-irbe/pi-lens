\* expect: violated NoSilentLoss
\* module: IdleReset
\* Fix mutant: only touchFile's checkDestroyed() early return is marked; the destroyed getClientForFile return still drops.
CONSTANTS
    Sweep = FALSE
    SweepOverrun = FALSE
    Reaping = FALSE
    Cascade = TRUE
    WarmAttach = FALSE
    HoldCheck = TRUE
    FixParts = {"entry"}
SPECIFICATION Spec
INVARIANTS TypeOK NoSilentLoss
CHECK_DEADLOCK FALSE
