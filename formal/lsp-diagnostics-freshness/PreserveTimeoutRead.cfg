\* expect: violated FreshRead
\* module: DiagnosticsFreshness
\* Versioned preserveDiagnostics resync: a wait that times out reads the kept pre-touch diagnostics (the #1095 note, client.ts ~4056).
CONSTANTS
    VersionedServer = TRUE
    SkippedWait = TRUE
    SeedFirstPush = FALSE
    AsyncServer = FALSE
    Preserve = TRUE
    Fence = FALSE
    ReplyFirst = FALSE
    MaxPubs = 3
    Mutant = "none"
SPECIFICATION Spec
INVARIANTS FreshRead
CHECK_DEADLOCK FALSE
