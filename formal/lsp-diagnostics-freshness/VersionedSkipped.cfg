\* expect: pass
\* module: DiagnosticsFreshness
\* Versioned server, the dispatch touch is skipped (shouldSkipNotify) and waits with no baseline (index.ts ~5727).
CONSTANTS
    VersionedServer = TRUE
    SkippedWait = TRUE
    SeedFirstPush = FALSE
    AsyncServer = TRUE
    Preserve = FALSE
    Fence = FALSE
    ReplyFirst = FALSE
    MaxPubs = 3
    Mutant = "none"
SPECIFICATION Spec
INVARIANTS FreshResult FreshRead NoKnownStaleCached
CHECK_DEADLOCK FALSE
