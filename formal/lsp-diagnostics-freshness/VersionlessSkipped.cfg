\* expect: pass
\* module: DiagnosticsFreshness
\* After #3484: version-less server, the skipped dispatch touch's no-baseline wait. The fenced client drops the pre-touch publish.
CONSTANTS
    VersionedServer = FALSE
    SkippedWait = TRUE
    SeedFirstPush = FALSE
    AsyncServer = FALSE
    Preserve = FALSE
    Fence = TRUE
    ReplyFirst = TRUE
    MaxPubs = 3
    Mutant = "none"
SPECIFICATION Spec
INVARIANTS FreshResult NoFreshDropped
CHECK_DEADLOCK FALSE
