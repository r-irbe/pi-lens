\* expect: violated FreshResult
\* module: DiagnosticsFreshness
\* Fix mutation: the fence is sent and armed after the didChange send resolves (a separate step).
CONSTANTS
    VersionedServer = FALSE
    SkippedWait = TRUE
    SeedFirstPush = FALSE
    AsyncServer = FALSE
    Preserve = FALSE
    Fence = TRUE
    ReplyFirst = TRUE
    MaxPubs = 3
    Mutant = "fenceLate"
SPECIFICATION Spec
INVARIANTS FreshResult
CHECK_DEADLOCK FALSE
