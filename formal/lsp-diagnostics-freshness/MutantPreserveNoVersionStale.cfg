\* expect: violated FreshResult
\* module: DiagnosticsFreshness
\* Non-vacuity: preserveDiagnostics resync with isVersionStale removed: the kept pre-touch diagnostics settle the wait at once.
CONSTANTS
    VersionedServer = TRUE
    SkippedWait = TRUE
    SeedFirstPush = FALSE
    AsyncServer = FALSE
    Preserve = TRUE
    Fence = FALSE
    ReplyFirst = FALSE
    MaxPubs = 3
    Mutant = "noVersionStale"
SPECIFICATION Spec
INVARIANTS FreshResult
CHECK_DEADLOCK FALSE
