\* expect: violated NoFreshDropped
\* module: DiagnosticsFreshness
\* Mutation: the fence on a server that publishes the new content before answering it (docker-langserver): its only fresh answer is dropped. Why the code fences only servers measured reply-first.
CONSTANTS
    VersionedServer = FALSE
    SkippedWait = TRUE
    SeedFirstPush = FALSE
    AsyncServer = FALSE
    Preserve = FALSE
    Fence = TRUE
    ReplyFirst = FALSE
    MaxPubs = 3
    Mutant = "none"
SPECIFICATION Spec
INVARIANTS NoFreshDropped
CHECK_DEADLOCK FALSE
