\* expect: pass
\* module: DiagnosticsFreshness
\* After #3484: version-less server, baseline wait. The fence goes out with the didChange (client.ts sendFenced) and the pre-touch publish is dropped until its reply.
CONSTANTS
    VersionedServer = FALSE
    SkippedWait = FALSE
    SeedFirstPush = FALSE
    AsyncServer = FALSE
    Preserve = FALSE
    Fence = TRUE
    ReplyFirst = TRUE
    MaxPubs = 3
    Mutant = "none"
SPECIFICATION Spec
INVARIANTS FreshResult NoFreshDropped
CHECK_DEADLOCK FALSE
