\* expect: pass
\* module: DiagnosticsFreshness
\* After #3484: version-less seed-first-push server, no-baseline wait. The pre-touch publish is dropped at receipt while the fence is out.
CONSTANTS
    VersionedServer = FALSE
    SkippedWait = TRUE
    SeedFirstPush = TRUE
    AsyncServer = FALSE
    Preserve = FALSE
    Fence = TRUE
    ReplyFirst = TRUE
    MaxPubs = 3
    Mutant = "none"
SPECIFICATION Spec
INVARIANTS FreshResult NoFreshDropped
CHECK_DEADLOCK FALSE
