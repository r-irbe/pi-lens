\* expect: pass
\* module: DocumentLifecycle
\* After #3477: a change queued before the rename, or a touch after it, is not sent for the closed path (was: fallback didOpen re-opened it).
CONSTANTS
    Touches = 1
    TouchAfterRename = TRUE
    StartOpen = TRUE
    QueuedClose = TRUE
SPECIFICATION Spec
INVARIANTS NoPhantomAfterRename
CHECK_DEADLOCK FALSE
