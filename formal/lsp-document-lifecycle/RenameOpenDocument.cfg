\* expect: pass
\* module: DocumentLifecycle
\* After #3477: rename's close is queued behind the change in flight, and the change queued behind it is not sent (was: didChange after didClose).
CONSTANTS
    Touches = 1
    TouchAfterRename = FALSE
    StartOpen = TRUE
    QueuedClose = TRUE
SPECIFICATION Spec
INVARIANTS LifecycleOrder NoPhantomAfterRename
CHECK_DEADLOCK FALSE
