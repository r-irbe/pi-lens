\* expect: pass
\* module: DocumentLifecycle
\* After #3477: rename closes every active client; the queued close waits for the in-flight didOpen and then closes it (was: nothing closed, path left open).
CONSTANTS
    Touches = 1
    TouchAfterRename = FALSE
    StartOpen = FALSE
    QueuedClose = TRUE
SPECIFICATION Spec
INVARIANTS LifecycleOrder NoPhantomAfterRename
CHECK_DEADLOCK FALSE
