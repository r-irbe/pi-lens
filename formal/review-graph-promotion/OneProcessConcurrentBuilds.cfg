\* expect: violated InProcessViewMonotone
\* module: ReviewGraphPromotion
\* The shipped code, one process, two builds with different changedFiles in flight (not deduped, buildCacheKey ~5967): the one that started first finishes last and persists the older view.
CONSTANTS
    Procs = {p1}
    MaxSeq = 2
    MaxBuilds = 2
    Conc = 2
    GenGate = TRUE
    SweepLiveness = TRUE
    AllowCrash = FALSE
    AllowWorkerDeath = FALSE
    FlushCurrentOnly = TRUE
SPECIFICATION Spec
INVARIANTS TypeOK InProcessViewMonotone
CHECK_DEADLOCK FALSE
