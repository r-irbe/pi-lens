\* expect: violated NoRegression
\* module: ReviewGraphPromotion
\* The shipped code, two processes (#3509 shape): a slow persist of the older view renames over a sibling's newer body.
CONSTANTS
    Procs = {p1, p2}
    MaxSeq = 2
    MaxBuilds = 1
    Conc = 1
    GenGate = TRUE
    SweepLiveness = TRUE
    AllowCrash = FALSE
    AllowWorkerDeath = FALSE
    FlushCurrentOnly = TRUE
SPECIFICATION Spec
INVARIANTS TypeOK NoRegression
CHECK_DEADLOCK FALSE
