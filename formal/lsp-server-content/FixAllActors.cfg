\* expect: pass
\* module: ServerContent
\* Candidate fix with both writes and the cascade reader (no in-flight prior entry, sweep only after quiescence, to stay in budget).
CONSTANTS
    Writes = 2
    Cascade = TRUE
    EqualLength = TRUE
    LossyFp = FALSE
    Debounce = TRUE
    RecentA = FALSE
    MidSweeps = 0
    PriorInFlight = FALSE
    FifoLanding = TRUE
    SweepKey = "both"
    FixCoalesce = TRUE
    FixDrop = TRUE
SPECIFICATION Spec
INVARIANTS ServerMatchesDisk SendsMonotone
CHECK_DEADLOCK FALSE
