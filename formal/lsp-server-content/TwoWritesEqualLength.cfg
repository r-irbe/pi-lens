\* expect: pass
\* module: ServerContent
\* Two same-turn pipeline touches of N (write-ordering-guard.ts ~6-13), after #3481: the older read is dropped or kept out by read order, not sent last.
CONSTANTS
    Writes = 2
    Cascade = FALSE
    EqualLength = TRUE
    LossyFp = FALSE
    Debounce = TRUE
    RecentA = FALSE
    MidSweeps = 1
    PriorInFlight = FALSE
    FifoLanding = TRUE
    SweepKey = "both"
    FixCoalesce = TRUE
    FixDrop = TRUE
SPECIFICATION Spec
INVARIANTS ServerMatchesDisk 
CHECK_DEADLOCK FALSE
