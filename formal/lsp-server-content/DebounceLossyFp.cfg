\* expect: pass
\* module: ServerContent
\* One edit inside the 1500 ms debounce window that changes only the middle of a >96-char file, after #3480: shouldSkipNotify and the sweep's confirmation read hash the whole text (document-drift.ts ~76), so the touch is sent.
CONSTANTS
    Writes = 1
    Cascade = FALSE
    EqualLength = TRUE
    LossyFp = FALSE
    Debounce = TRUE
    RecentA = TRUE
    MidSweeps = 1
    PriorInFlight = FALSE
    FifoLanding = TRUE
    SweepKey = "both"
    FixCoalesce = FALSE
    FixDrop = FALSE
SPECIFICATION Spec
INVARIANTS ServerMatchesDisk 
CHECK_DEADLOCK FALSE
