\* expect: pass
\* module: ServerContent
\* Candidate fix (last-READ-wins queue) on the cascade race, with an earlier notify of N still in flight so the coalescing path is live.
CONSTANTS
    Writes = 1
    Cascade = TRUE
    EqualLength = TRUE
    LossyFp = FALSE
    Debounce = TRUE
    RecentA = FALSE
    MidSweeps = 1
    PriorInFlight = TRUE
    FifoLanding = TRUE
    SweepKey = "both"
    FixCoalesce = TRUE
    FixDrop = TRUE
SPECIFICATION Spec
INVARIANTS ServerMatchesDisk SendsMonotone
CHECK_DEADLOCK FALSE
