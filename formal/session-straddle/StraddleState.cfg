\* expect: pass
\* module: SessionStraddle
\* Shipped code (#3499): session 1's quiet window is still settling when session 2's reset runs; its late session-1 writes drop and session 2's own touch survives.
CONSTANTS
    QuietWindow = TRUE
    ResetClears = TRUE
    Dedupe = TRUE
    ToolDrift = FALSE
    Strays = FALSE
    FixParts = {"settle","reconcile","reconcileTaskCapture"}
SPECIFICATION Spec
INVARIANTS TypeOK NoCrossSessionState NoDropFreshTouch
CHECK_DEADLOCK FALSE
