\* expect: violated NoCrossSessionDelivery
\* module: SessionStraddle
\* Residual (#3512): a session-1 compute records a touch after the reset. The shipped code cannot tell it from session 2's own and delivers it in session 2.
CONSTANTS
    QuietWindow = TRUE
    ResetClears = TRUE
    Dedupe = TRUE
    ToolDrift = FALSE
    Strays = TRUE
    FixParts = {"settle","reconcile","reconcileTaskCapture"}
SPECIFICATION Spec
INVARIANTS TypeOK NoCrossSessionDelivery
CHECK_DEADLOCK FALSE
