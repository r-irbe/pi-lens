\* expect: violated NoCrossSessionState
\* module: SessionStraddle
\* Guard mutant of the shipped code: the reset clear is still needed for state parked before the replacement.
CONSTANTS
    QuietWindow = TRUE
    ResetClears = FALSE
    Dedupe = TRUE
    ToolDrift = FALSE
    Strays = FALSE
    FixParts = {"settle","reconcile","reconcileTaskCapture"}
SPECIFICATION Spec
INVARIANTS TypeOK NoCrossSessionState NoCrossSessionDelivery
CHECK_DEADLOCK FALSE
