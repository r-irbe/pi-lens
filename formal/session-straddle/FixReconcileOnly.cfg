\* expect: violated NoCrossSessionState
\* module: SessionStraddle
\* Fix mutant: only the reconcile is guarded.
CONSTANTS
    QuietWindow = TRUE
    ResetClears = TRUE
    Dedupe = TRUE
    ToolDrift = FALSE
    Strays = FALSE
    FixParts = {"reconcile","reconcileTaskCapture"}
SPECIFICATION Spec
INVARIANTS TypeOK NoCrossSessionState NoCrossSessionDelivery
CHECK_DEADLOCK FALSE
