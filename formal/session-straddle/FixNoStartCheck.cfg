\* expect: violated NoDropFreshTouch
\* module: SessionStraddle
\* Capture mutant (the round-0 code): the reconcile captures at window start. A reconcile that starts after the reset drains session 2's own touch and its append guard drops it.
CONSTANTS
    QuietWindow = TRUE
    ResetClears = TRUE
    Dedupe = TRUE
    ToolDrift = FALSE
    Strays = FALSE
    FixParts = {"settle","reconcile"}
SPECIFICATION Spec
INVARIANTS TypeOK NoCrossSessionState NoDropFreshTouch
CHECK_DEADLOCK FALSE
